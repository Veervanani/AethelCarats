{
  "name": "aethelcarats",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AethelCarats Luxury Fine Jewellery & Diamond Vault Platform",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "build:backend": "cd backend && npm install --include=dev && npx prisma generate && npm run build",
    "build": "npm run build:frontend && npm run build:backend && node scripts/verify-build.js",
    "start": "node server.js",
    "start:frontend": "cd frontend && node server.js",
    "start:backend": "cd backend && npm start",
    "db:migrate": "cd backend && npx prisma migrate dev",
    "db:seed": "cd backend && npx prisma db seed",
    "db:generate": "cd backend && npx prisma generate"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.21.0",
    "@paypal/checkout-server-sdk": "^1.0.3",
    "@prisma/client": "^5.18.0",
    "axios": "^1.7.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "jszip": "^3.10.1",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.24.3",
    "nodemailer": "^6.9.14",
    "pdfkit": "^0.19.1",
    "xlsx": "^0.18.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

